{
  "name": "zenway-saint-laurent-api",
  "private": true,
  "description": "Dépendances des fonctions serverless api/ (événements, planning, infos pratiques) et outillage de qualité (Prettier, ESLint). Le site public (index.html + assets/) reste statique, sans build step.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "verifier": "npm run format:check && npm run lint"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "@vercel/blob": "^2.8.0",
    "google-auth-library": "^10.9.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "globals": "^16.5.0",
    "prettier": "^3.9.6"
  }
}
